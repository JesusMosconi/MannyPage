import { planes } from "@/datos/planes";import { TarjetaPlan } from "@/componentes/ui/TarjetaPlan";
export function Planes(){return <section className="bg-fondo py-24 md:py-30"><div className="contenedor"><div id="planes" className="mb-16 text-center"><h2 className="titulo">PLANES DE <span className="text-primario-claro">ENTRENAMIENTO</span></h2><p className="mx-auto mt-4 max-w-xl text-texto-secundario">Elegí el nivel de acompañamiento que necesitás. Todos los planes se adaptan a tu punto de partida.</p></div><div className="grid gap-7 md:grid-cols-3 md:items-stretch">{planes.map(p=><TarjetaPlan key={p.id} plan={p}/>)}</div></div></section>}
