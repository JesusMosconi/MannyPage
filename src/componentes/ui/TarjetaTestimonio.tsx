import { Star } from "lucide-react";import type { Testimonio } from "@/tipos";
export function TarjetaTestimonio({testimonio}:{testimonio:Testimonio}){return <article className="h-full rounded-tarjeta border border-superficie-alta bg-superficie-baja p-7"><div className="mb-5 flex items-center gap-4"><div className="grid h-12 w-12 place-items-center rounded-full bg-primario font-bold text-primario-oscuro">{testimonio.foto}</div><div><h3 className="font-bold">{testimonio.nombre}</h3><div className="flex gap-1">{Array.from({length:5},(_,i)=><Star key={i} className={`h-4 w-4 ${i<testimonio.calificacion?"fill-primario text-primario":"text-superficie-alta"}`}/>)}</div></div></div><p className="italic leading-relaxed text-texto-secundario">“{testimonio.comentario}”</p></article>}
