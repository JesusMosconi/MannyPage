import { Quote } from "lucide-react";import { obtenerIniciales } from "@/lib/texto";import type { Testimonio } from "@/tipos";
export function TarjetaTestimonio({testimonio}:{testimonio:Testimonio}){return <article className="flex h-full flex-col overflow-hidden rounded-tarjeta border border-superficie-alta bg-superficie-baja"><div className="aspect-[4/3] bg-superficie-alta">{testimonio.resultado?.tipo==="foto"?<img src={testimonio.resultado.imagen} alt={`Resultado de ${testimonio.nombre}`} className="h-full w-full object-cover object-center"/>:testimonio.resultado?.tipo==="antes-despues"?<div className="grid h-full grid-cols-2"><div className="relative overflow-hidden"><img src={testimonio.resultado.antes} alt={`Antes de ${testimonio.nombre}`} className="h-full w-full object-cover"/><span className="absolute bottom-2 left-2 rounded bg-black/60 px-2 py-1 text-xs text-white">Antes</span></div><div className="relative overflow-hidden"><img src={testimonio.resultado.despues} alt={`Después de ${testimonio.nombre}`} className="h-full w-full object-cover"/><span className="absolute right-2 bottom-2 rounded bg-black/60 px-2 py-1 text-xs text-white">Después</span></div></div>:<div className="grid h-full place-items-center"><Quote className="size-10 text-primario opacity-40"/></div>}</div><div className="flex flex-1 flex-col p-6"><div className="flex items-center gap-3">{testimonio.foto?<img src={testimonio.foto} alt={testimonio.nombre} className="h-10 w-10 shrink-0 rounded-full object-cover"/>:<div className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-primario font-bold text-primario-oscuro">{obtenerIniciales(testimonio.nombre)}</div>}<div><h3 className="font-bold text-texto">{testimonio.nombre}</h3>{testimonio.linkUsuario?<a href={testimonio.linkUsuario} target="_blank" rel="noopener noreferrer" className="text-sm text-primario-claro hover:underline">@{testimonio.usuario}</a>:<p className="text-sm text-primario-claro">@{testimonio.usuario}</p>}</div></div><p className="mt-4 italic leading-relaxed text-texto-secundario">“{testimonio.comentario}”</p></div></article>}
