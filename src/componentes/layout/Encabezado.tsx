"use client";
import { Menu, X } from "lucide-react";
import { useState } from "react";
const enlaces=[{texto:"Sobre mí",href:"#sobre-mi"},{texto:"Testimonios",href:"#testimonios"},{texto:"Planes",href:"#planes"}];
export function Encabezado(){const [abierto,setAbierto]=useState(false);return <header className="fixed inset-x-0 top-0 z-50 border-b border-white/10 bg-fondo/85 backdrop-blur-xl"><div className="contenedor flex h-20 items-center justify-between"><a href="#inicio" className="font-titulos text-3xl tracking-wider text-primario">IRON COACH</a><nav className="hidden items-center gap-8 md:flex" aria-label="Navegación principal">{enlaces.map(e=><a key={e.texto} className="text-sm font-bold text-texto-secundario hover:text-primario" href={e.href}>{e.texto}</a>)}</nav><a href="#contacto" className="boton boton-primario hidden md:inline-flex">EMPEZAR AHORA</a><button className="text-primario md:hidden" onClick={()=>setAbierto(!abierto)} aria-label="Abrir menú" aria-expanded={abierto}>{abierto?<X/>:<Menu/>}</button></div>{abierto&&<nav className="border-t border-white/10 bg-fondo px-5 py-5 md:hidden">{enlaces.map(e=><a key={e.texto} onClick={()=>setAbierto(false)} className="block border-b border-white/10 py-4 font-bold" href={e.href}>{e.texto}</a>)}<a onClick={()=>setAbierto(false)} href="#contacto" className="boton boton-primario mt-5 w-full">EMPEZAR AHORA</a></nav>}</header>}
